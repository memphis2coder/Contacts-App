// place to store the todos
let todos = [];

// the function to add new todos
function addTodo(text) {
    // create a new object
    const todo = {
        text,
        checked: false,
        id: Date.now()
    };
}


